{
    "version": 2,
    "builds": [
      {
        "src": "main.py",
        "use": "@vercel/python",
        "config": {
          "command": "pip install -r requirements.txt && FLASK_APP=main.py FLASK_ENV=production flask run --host=0.0.0.0 --port=$PORT"
        }
      }
    ],
    "routes": [
      {
        "src": ".*",
        "dest": "main.py"
      }
    ]
  }
  
